{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Учёт расходов команды — Node.js/Express + PostgreSQL (Railway)",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server/index.js",
    "migrate": "node server/migrate.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}
